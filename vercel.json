{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/agent/:action", "destination": "/api/agent/:action" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
